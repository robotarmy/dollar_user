if has('gui_running')
  set background=light
else
  set background=dark
endif

let g:solarized_contrast='high'
colorscheme solarized
"colorscheme solarized_contrast

" introducing hamlc - coffeescript haml that looks like ruby haml
au BufRead,BufNewFile *.hamlc set ft=haml
